apiVersion: v1
kind: Service
metadata:
  name: {{.Values.name}}
  namespace: {{.Values.namespace}}
spec:
  ports:
  - port: 80
    targetPort: 9000
    protocol: TCP
  selector:
    app: {{.Values.name}}
